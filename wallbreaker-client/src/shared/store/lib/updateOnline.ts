// import { IConversation, IConversationOnline } from 'shared/types/conversations'

// export default function updateOnline(
//     conversations: IConversation[],
//     conversationsOnline: IConversationOnline[]
// ) {
//     return conversations.map(conversation => {
//         conversation.lastOnline = conversationsOnline.find(c => c.id === conversation.id).lastOnline
//         return conversation
//     })
// }
